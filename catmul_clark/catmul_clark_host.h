#pragma once 

#include "catmul_clark.h"

// your host code (preprocessing functions, may be file reading, etc)

/* Example
  struct PreprocessedData 
  {
  };

  PreprocessedData calmul_clark_preprocessing(const CatmulClark &surface);

  CatmulClark load_from_file(const std::string &path);

  ...
*/